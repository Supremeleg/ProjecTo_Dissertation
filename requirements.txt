# ProjecTo 智能交互投影系统 - 依赖文件
# 基于 LeRobot SoArm101 项目优化

# ============================================================================
# 核心UI框架
# ============================================================================
PyQt6>=6.4.0

# ============================================================================
# 计算机视觉和图像处理
# ============================================================================
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0

# 深度学习框架（用于YOLO物体检测）
torch>=2.2.1,<2.7
torchvision>=0.21.0

# YOLO物体检测
ultralytics>=8.0.0

# 图像和视频处理
imageio[ffmpeg]>=2.34.0
av>=14.2.0

# ============================================================================
# 机械臂控制（可选）
# ============================================================================
# LeRobot 核心框架（可选安装）
# lerobot>=0.1.0

# 舵机控制SDK（根据机械臂型号选择）
# SO101使用Feetech舵机：
# feetech-servo-sdk>=1.0.0

# 其他机械臂使用Dynamixel舵机：
# dynamixel-sdk>=3.7.31

# 串口通信
pyserial>=3.5

# 键盘控制（用于调试和手动控制）
pynput>=1.7.7

# ============================================================================
# 配置管理和数据处理
# ============================================================================
# 配置文件处理
omegaconf>=2.3.0
draccus>=0.10.0

# 数据处理
h5py>=3.10.0
zarr>=2.17.0
jsonlines>=4.0.0

# ============================================================================
# 网络和通信
# ============================================================================
# HTTP客户端
requests>=2.31.0

# WebSocket（可选，用于远程控制）
# websockets>=11.0.0

# ============================================================================
# 实验跟踪和调试（可选）
# ============================================================================
# 实验跟踪
# wandb>=0.16.3

# 可视化调试
# rerun-sdk>=0.21.0

# ============================================================================
# 系统工具
# ============================================================================
# 包管理
packaging>=24.2

# 数学计算
einops>=0.8.0

# 终端输出
termcolor>=2.4.0

# 文件监控
# watchdog>=3.0.0

# ============================================================================
# 开发工具（开发环境可选）
# ============================================================================
# 代码质量
# pytest>=8.1.0
# pytest-cov>=5.0.0
# pre-commit>=3.7.0

# 调试工具
# debugpy>=1.8.1

# ============================================================================
# 平台特定依赖
# ============================================================================
# Windows特定
# pywin32>=306; sys_platform == "win32"

# Linux特定
# python-evdev>=1.6.0; sys_platform == "linux"

# ============================================================================
# 安装说明
# ============================================================================
# 基础安装（仅视觉交互）：
#   pip install -r requirements.txt
#
# 完整安装（包含机械臂支持）：
#   pip install -r requirements.txt
#   pip install lerobot>=0.1.0
#   pip install feetech-servo-sdk>=1.0.0  # 对于SO101
#   # 或
#   pip install dynamixel-sdk>=3.7.31     # 对于其他机械臂
#
# 开发环境安装：
#   pip install -r requirements.txt
#   pip install pytest pytest-cov pre-commit debugpy
#
# 实验环境安装（包含所有可选依赖）：
#   pip install -r requirements.txt
#   pip install lerobot[feetech] wandb rerun-sdk websockets
